# AI Drama Studio — production is static demo (shorts.fortunesite.one).
# Full generation backend is DEFERRED (needs paid API authorization).

# Demo / static: no secrets required.

# Future full stack (do not enable without written auth):
# DATABASE_URL=
# JWT_SECRET=
# STRIPE_SECRET_KEY=
# KLING_API_KEY=
# LAOZHANG_API_KEY=
# VITE_OAUTH_PORTAL_URL=
# VITE_APP_ID=
