# MoYu — production is static GH Pages (chillworks.ai). No server secrets required.
# Local / future light backend (SX2-2) only:

# PORT=3000
# DATABASE_URL=file:./moyu.db
# OPENAI_API_KEY=   # optional AI copy; fallback to local quotes

# Manus publish placeholders (do not commit real values)
# VITE_ANALYTICS_ENDPOINT=
# VITE_ANALYTICS_WEBSITE_ID=
# VITE_OAUTH_PORTAL_URL=
# VITE_APP_ID=
