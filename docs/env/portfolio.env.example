# Portfolio — production is static GH Pages (rj.fortunesite.one).
# Use: PORTFOLIO_STATIC=1 pnpm exec vite build  (skips Manus runtime)

# Optional Manus publish only:
# VITE_ANALYTICS_ENDPOINT=
# VITE_ANALYTICS_WEBSITE_ID=
# VITE_OAUTH_PORTAL_URL=
# VITE_APP_ID=
