# Live bugs — quantradar.one (2026-07-12)

Evidence collected against production `https://quantradar.one` (Manus SPA + tRPC).  
Local GitHub shell (`python -m app`) is **not** what is currently deployed (`/health` shapes differ).

## Deployed stack (observed)

| Item | Live |
|------|------|
| UI | Vite/React SPA (`/assets/index-*.js`) |
| API | tRPC `/api/trpc/*` |
| Health | `{"ok":true,"service":"quantradar","env":"production"}` |
| Auth | Still builds `manus.im/app-auth?appId=2SSXJznArC9L4pnEj3J3KF` |
| Analysis | `POST /api/trpc/quant.analyzeStock` body `{"json":{"symbol","includeOptions",...}}` |

## Confirmed bugs

| ID | Sev | Symptom | Evidence |
|----|-----|---------|----------|
| BUG-1 | P0 | Fake ticker `XXXX` returns score 76 + **buy** + options | multi-ticker probe 2026-07-12 |
| BUG-2 | P0 | Top-level `volumeAnalysis.avgVolume20/currentVolume` often **0** while nested daily volume is real; validation still invents narratives | AAPL top-level vol=0 vs `technicalIndicators.volume.volume=34109200` |
| BUG-3 | P0 | Options: `expectedProfitPercent=1993`, strike reason vs contract mismatch, DTE vs recommendedExpiry conflict, always `optionChainDataSource=simulated` | NVDA `includeOptions=true` |
| BUG-4 | P0 | `BRK.B` → HTTP 500 `No data returned`; `BRK-B` partial OK | direct curl |
| BUG-5 | P1 | `sectorGate` always `null` despite smartGate copy about sector ETF | all tickers tested |
| BUG-6 | P1 | `patternNameCn` / strategy Cn often English clone; `efficiencyData.patternNameCn` correct | AAPL Double Top |
| BUG-7 | P1 | Conflicting signals: e.g. TSLA `action=buy` + `primary=no_trade`; summary “Wait & Watch” vs avoid | AAPL/TSLA |
| BUG-8 | P2 | Float noise `21.599999999999998` | technicalScore.breakdown |
| BUG-9 | P1 | Product still depends on Manus platform login | main bundle `wh()` |
| BUG-10 | P2 | `getPublicStats` → 1247 / 896 (likely static social proof) | GET quant.getPublicStats |
| BUG-11 | P2 | Unknown `/api/*` soft-404 as SPA HTML 200 | `/api/analyze` etc. |
| BUG-12 | P1 | `currentVolume=0` even when `avgVolume20>0` (GOOGL/SPY/BRK-B) | multi-ticker |

## Fix channel

Source of the live SPA is **Manus project** `Ut7yyEov4qxwFVSYULmSsP` (quantradar.one), not `~/quantradar` shell.  
A Manus task was filed with full reproduction + acceptance checklist (see `PROGRESS.md`).

**Path-C fix (2026-07-12, repo v0.2.0):** GitHub shell now implements fail-closed P0 gates
(`app/quality.py`, honest UI, `scripts/p0_smoke.py`).  
**Production still needs cutover** — see [P0_CUTOVER.md](./P0_CUTOVER.md). Until `/health`
returns `service=quantradar-shell`, live Manus SPA bugs above remain user-visible.

## Smoke commands

```bash
curl -s https://quantradar.one/health
curl -s 'https://quantradar.one/api/trpc/quant.getPublicStats'
curl -s -X POST https://quantradar.one/api/trpc/quant.analyzeStock \
  -H 'content-type: application/json' \
  -d '{"json":{"symbol":"AAPL","includeOptions":true,"skipGateCheck":false,"riskPreference":"balanced","enableLLMValidation":false}}'
```
