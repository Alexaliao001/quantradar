# QuantRadar build 9 — 2026-09-06

This update replaces the quick-tag-only journal with a free, native written-plan and process-review workflow. Plan is the default tab. Saving preserves the original reason, trigger, invalidation and review date; a single later review is appended separately. Existing journal data is retained, same-day entries remain distinct, and new entries no longer silently evict older ones.

The scan permission check now follows current StoreKit entitlement state, including changes while a request is running. Unavailable radar data consistently displays and records UNKNOWN / PAUSE. The onboarding page scrolls at accessibility text sizes.

## Verified

- Python: 244 tests passed; P0 smoke and contract validation passed.
- Core iOS: 52 tests, 4 live-provider checks deliberately skipped, zero failures.
- Unpurchased plan → save → review → relaunch flow passed on iPhone 17 Pro, iPhone 13 Pro Max and iPad Pro 12.9-inch simulators.
- The six PNGs in `iphone/` and `ipad/` are unedited native screenshots from the tested build 9 code. EXAMPLE and its practice text were entered through the UI. No market or return claim is implied.
- Release archive: app and widget 1.2.0 (9), valid signatures, no debug launch hooks or StoreKit test configuration in the shipped app.
- Maximum accessibility text size: onboarding copy scrolls without truncation and Start my plan remains usable. Native record sharing opens the expected text preview; no recipient was selected.
- [GitHub CI 34025153542](https://github.com/Alexaliao001/quantradar/actions/runs/34025153542): Python and iOS unit jobs SUCCESS at source commit `73b6e311c7ac3261cd9d11159f5b0c06998fa84a`.

## Purchase validation — BLOCKED, not passed

The StoreKit framework tests are in the separate `QuantRadarStoreKit` scheme, so deterministic core tests and environment-dependent purchase acceptance report independently. They call the production PurchaseStore methods for cancellation, purchase, restore, refund and pending approval; no forced entitlement is used. Failed configuration activation throws immediately rather than allowing a false cancellation pass.

On this host (Xcode 26.6, iOS 26.5 runtime 23F77), SKTestSession configuration mutations fail with SKInternalErrorDomain Code 3. The same failure occurs with an untouched configuration generated by Xcode and when tests are started from the Xcode IDE. This is an environment failure; it does not establish successful purchase/restore/refund behavior. The old configuration also failed to open in the current Xcode editor. Its replacement follows the generated format and contains only the currently offered Unlock test product. Local test price is $9.99; it does not change the ASC price.

Rerun on a compatible Xcode/runtime before public release:

```sh
cd ios
xcodegen generate
xcodebuild test -project QuantRadar.xcodeproj -scheme QuantRadarStoreKit \
  -destination 'platform=iOS Simulator,id=<dedicated-test-device>'
```

A TestFlight sandbox purchase, purchase-sheet cancellation and restart/restore check also remain outstanding. Commercial permission for the current Yahoo/Nasdaq market-data delivery remains unverified. Manual release is required while these gates remain open. Review submission, Apple approval and public availability are separate states.

## App Store materials

`listing.json` contains the subtitle, description, promotional text and factual review steps now saved in App Store Connect. All six screenshots were uploaded and verified COMPLETE against their local file checksums before the old screenshots were removed.

## Verified submission — 2026-09-06 17:45 Asia/Shanghai

- App Store version **1.2.0**, selected **build 9**, processing state **VALID**.
- New submission **07500692-0e8e-4347-b06f-395810c8d5b0**, submitted at **2026-09-06T09:45:12.963Z**.
- Both **iOS App 1.2.0 (9)** and **QuantRadar Unlock** are **WAITING_FOR_REVIEW**, verified independently through the API and Safari page. The prior build 7 submission was canceled before replacement.
- Release type is **MANUAL**. This is a resubmission following the August 28 Guideline 4.3(a) feedback, not approval or public availability. Purchase acceptance and commercial data permission remain open as described above.

[App Store Connect submission](https://appstoreconnect.apple.com/apps/6800090745/distribution/reviewsubmissions/details/07500692-0e8e-4347-b06f-395810c8d5b0) · [Sanitized receipt](submission-receipt.json)

Full private API, archive and upload receipts remain under `ios/build/ios43-release-20260906/` and are not committed.
